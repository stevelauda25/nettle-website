import { Features } from "@/components/sections/features";

/** The production Features, exactly as the homepage renders it. Server component. */
export function Original() {
  return <Features />;
}
