import { BusinessLines } from "@/components/sections/business-lines";

/** The production Business lines, exactly as the homepage renders it. Server component. */
export function Original() {
  return <BusinessLines />;
}
