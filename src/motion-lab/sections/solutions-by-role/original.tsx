import { SolutionsByRole } from "@/components/sections/solutions-by-role";

/** The production Solutions by role, exactly as the homepage renders it. Server component. */
export function Original() {
  return <SolutionsByRole />;
}
