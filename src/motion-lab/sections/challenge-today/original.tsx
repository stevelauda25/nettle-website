import { ChallengeToday } from "@/components/sections/challenge-today";

/** The production Challenge today, exactly as the homepage renders it. Server component. */
export function Original() {
  return <ChallengeToday />;
}
