import { Testimonial } from "@/components/sections/testimonial";

/** The production Testimonial, exactly as the homepage renders it. Server component. */
export function Original() {
  return <Testimonial />;
}
