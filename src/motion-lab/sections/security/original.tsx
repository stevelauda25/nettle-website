import { Security } from "@/components/sections/security";

/** The production Security, exactly as the homepage renders it. Server component. */
export function Original() {
  return <Security />;
}
