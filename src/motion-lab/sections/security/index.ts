import type { SectionMeta } from "../../types";

/** Security — metadata only. Concepts are added here after their storyline is approved. */
const section: SectionMeta = {
  id: "security",
  title: "Security",
  summary: "Heading, supporting copy and three security cards with badge artwork on a black stage.",
  kind: "section",
  componentName: "Security",
  path: "src/components/sections/security/security.tsx",
  concepts: [],
};

export default section;
