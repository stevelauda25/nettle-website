import { KeyMetrics } from "@/components/sections/key-metrics/key-metrics";

/** The production Key metrics, exactly as the homepage renders it. Server component. */
export function Original() {
  return <KeyMetrics />;
}
