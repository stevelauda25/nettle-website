import type { SectionMeta } from "../../types";

/** Key metrics — metadata only. Concepts are added here after their storyline is approved. */
const section: SectionMeta = {
  id: "key-metrics",
  title: "Key metrics",
  summary: "Heading and three metric cards, each with an engraved bar.",
  kind: "section",
  componentName: "KeyMetrics",
  path: "src/components/sections/key-metrics/key-metrics.tsx",
  concepts: [],
};

export default section;
