import type { SectionMeta } from "../../types";

/** Video explainer — metadata only. Concepts are added here after their storyline is approved. */
const section: SectionMeta = {
  id: "video-explainer",
  title: "Video explainer",
  summary: "Poster with a play control. The only part of the homepage with existing motion: the poster scales in on a scroll timeline.",
  kind: "section",
  componentName: "VideoExplainer",
  path: "src/components/sections/video-explainer/video-explainer.tsx",
  concepts: [],
};

export default section;
