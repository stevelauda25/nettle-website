import { VideoExplainer } from "@/components/sections/video-explainer/video-explainer";

/** The production Video explainer, exactly as the homepage renders it. Server component. */
export function Original() {
  return <VideoExplainer />;
}
