import type { SectionMeta } from "../../types";

/** Hero — metadata only. Concepts are added here after their storyline is approved. */
const section: SectionMeta = {
  id: "hero",
  title: "Hero",
  summary: "Headline, subtitle and call to action, two brand accent compositions, and the product dashboard illustration.",
  kind: "section",
  componentName: "Hero",
  path: "src/components/sections/hero/hero.tsx",
  concepts: [],
};

export default section;
