import type { SectionMeta } from "../../types";

/** Site header — metadata only. Concepts are added here after their storyline is approved. */
const section: SectionMeta = {
  id: "site-header",
  title: "Site header",
  summary: "Announcement bar and primary navigation; sticky above every section.",
  kind: "layout",
  componentName: "SiteHeader",
  path: "src/components/layout/site-header.tsx",
  concepts: [],
};

export default section;
