import { SiteHeader } from "@/components/layout/site-header";

/** The production Site header, exactly as the homepage renders it. Server component. */
export function Original() {
  return <SiteHeader />;
}
